"""lynx_compare.render — presentation and serialization of results.

This subpackage turns :class:`~lynx_compare.core.engine.ComparisonResult`
objects into human-facing output: Rich terminal rendering
(:mod:`~lynx_compare.render.display`) and HTML/PDF/plain-text export
(:mod:`~lynx_compare.render.export`). It depends on
:mod:`lynx_compare.core` but never the reverse.
"""
