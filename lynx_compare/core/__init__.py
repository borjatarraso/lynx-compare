"""lynx_compare.core — pure comparison logic and package metadata.

This subpackage holds the domain layer: the binary comparison engine
(:mod:`~lynx_compare.core.engine`), the N-way extension
(:mod:`~lynx_compare.core.multi`), and project/metadata constants
(:mod:`~lynx_compare.core.about`). It performs no terminal, GUI, file, or
network I/O.
"""
